import {UIMode} from "types/common";


const config = {
    basename: '',
    fontFamily: `'Roboto', sans-serif`,
    borderRadius: 12,
    outlinedFilled: true,
    theme: "light" as UIMode,
};

export default config;
